# Dc-Dev Superflow Fixtures

This additive fixture namespace is reserved for deterministic superflow contract inputs. Current tests use inline values; this directory exists so T-014 does not claim a missing fixture path.
