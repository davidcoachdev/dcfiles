# Dc-Dev Skill Resolution

The project skill registry was read from `context/refs/kit-index.json`.

## Resolved

- Cavekit gold loop
- Gentle orchestrator protocols
- Cavekit Make
- Cavekit Check
- Autonomous loop
- Implementation tracking
- Validation-first

## Unresolved

The capability-discovery helper advertised by the capability-discovery skill is
not present at its expected path. Resolution is therefore `partial` and the
runtime capability artifact records `setup-required`; no unavailable tool is
represented as available.
