# Dc-Dev Superflow — Next Check Input

## Scope

Review only the additive Dc-Dev superflow namespace and its already-authorized additive registration. Do not edit or reinterpret legacy Cavekit, Gentle, SDD, `dc-dev`, legacy kits, or legacy tests.

## Required evidence

1. `context/dc-dev-superflow/kits/` contains exactly 10 superflow kits; `context/kits/` contains no `dc-dev-superflow-*` files.
2. Deterministic coverage parser reports new corpus 41 requirements / 125 acceptance criteria and legacy corpus 25 / 88 separately.
3. New suite is green and legacy suite is run separately and reported literally.
4. Compression shows measurable non-authoritative reduction, preserves reversible originals, uses literal transport for security/approval/receipt/evidence/provenance/irreversible content, and returns `setup-required` or safe no-compression when requested helpers are unavailable.
5. Result Contract rejects missing/invalid corpus counts and carries both corpus counts plus test counts.
6. No forbidden executable dependency or hidden fallback; scanner references used only to enforce denial are acceptable.
7. Config diff against `opencode.json.bak-dc-dev-superflow-20260818` remains 8 additive paths, zero removals, zero value changes.
8. Runtime loader/dispatch, recovery, provenance, TTS isolation, model/capability setup-required, and remaining I/V/T task coverage are explicitly classified PASS, OPEN, or INCONCLUSIVE with evidence.

## Current known open work

- Runtime loader/dispatch proof remains open.
- Full I/V-001–I/V-041 and T-061–T-093 implementation/validation remains partial or not started.
- Publication remains locked until open gates have evidence.
