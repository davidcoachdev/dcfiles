// role: checkpoint and recover failed or interrupted runs while preserving lineage
// Result Contract: status, recovery state, lineage, checkpoint, next action, provenance
export const role = "recovery"
