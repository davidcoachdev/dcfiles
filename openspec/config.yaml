# SDD Configuration — dcfiles
schema: openspec/v1

project:
  name: dcfiles
  type: dotfiles-manager
  description: Gestor de dotfiles + repositorio de dotfiles personales

stack:
  primary: bash
  note: Pure Bash MVP — symlink engine, CLI dispatcher, bootstrap installer. Ready for Rust migration.

testing:
  framework: bats-core
  command: bats test/
  strict_tdd: false

artifact_store:
  mode: both
  openspec_root: openspec/
  engram_project: dcfiles

session:
  execution_mode: interactive
  delivery_strategy: auto-forecast
  chain_strategy: feature-branch-chain
  review_budget_lines: 2000

generated: 2026-06-03T00:00:00Z
archived: 2026-06-03T18:50:00Z
